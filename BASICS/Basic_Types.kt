// Every variable anda data structure in Kotlin has a typer. Types are important because they tell 
// the compiler what yypu are allowed to do with that variable or data structure. in other words, 
// what a functions and properties it has 

// In the last example was able to tell in the last example that customers are type
// Int Kotlins ability to infer the types is called types inference. customers is assigned an
// interger value. From this, Kotlin infers that customers, has a numerical type Int. As a result,
// the compiler knows that you can perfrom arithmetics operations with customers 

